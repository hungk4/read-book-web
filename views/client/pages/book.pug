html
  head
    title= book.title
  body
    h1= book.title
    h3 Tác giả: #{book.author}
    if book.file
      iframe(src=`/books/${book.file}#page=${pageNumber}` width="100%" height="600px")
    else
      p Sách chưa có file đọc trực tuyến.
    a(href="/") Quay lại trang chủ